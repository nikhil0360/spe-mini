# playbook.yml
---
- hosts: host_machine
  tasks:
    - name: deploy flask-calc docker container locally
      docker_container:
        image: nikhil0360/flask-calc
        name: flask-calc
        state: started
        auto_remove: true
        ports:
          - "5000:5000"


# ansible plugin
# inventory file
# deploy on actual server